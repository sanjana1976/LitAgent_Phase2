# Transcripts (manual only)

The CLI does **not** write files here. Chat history is in SQLite (`conversation_history`, see [design.md](../design.md)).

Use `src/agent/chat_transcript.py` if you want to hand-author JSON logs (e.g. Cursor agent development notes). Example shape: `chat4.json`.
